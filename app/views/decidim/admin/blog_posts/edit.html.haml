= decidim_form_for(@blog_post, url: post_path(@blog_post), html: { class: 'form' }) do |f|
  .card
    .card__content
      .card-divider
        %h2.card-title
          Blogpost bearbeiten

      .card-section
        .row.column
          = f.text_field :title

        .row.column
          .field
            = f.editor :body, lines: 8, toolbar: :full

  .button--double
    = f.submit 'Blogpost speichern', class: 'button', data: { disable_with: true }
    = link_to 'Abbrechen', posts_path, class: 'button'
